module.exports = {
	// Specify the root directory for Jest to look for test files
	roots: ["<rootDir>/src"],
	preset: "ts-jest",
	// Specify the test environment (e.g., node, jsdom)
	testEnvironment: "node",

	// Add any other Jest configuration options as needed
};
